% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/llm_stance.R, R/methods.R
\name{llm_stance}
\alias{llm_stance}
\alias{llm_stance.character}
\alias{llm_stance.data.frame}
\title{Detect stance using COLA framework}
\usage{
llm_stance(x, ...)

\method{llm_stance}{character}(
  x,
  target,
  chat_base,
  type = c("object"),
  language = stancer_available_languages(),
  scale = c("categorical", "numeric", "likert"),
  domain_role = NULL,
  prompts_dir = NULL,
  verbose = TRUE,
  rpm = 20,
  ...
)

\method{llm_stance}{data.frame}(
  x,
  text = NULL,
  target = NULL,
  type = NULL,
  domain_role = NULL,
  language = NULL,
  chat_base,
  scale = "categorical",
  prompts_dir = NULL,
  verbose = TRUE,
  rpm = 20,
  .output_col = ".stance",
  ...
)
}
\arguments{
\item{x}{Input text(s) as a character vector or a data.frame containing (any of) \code{text}, \code{target}, \code{type}, \code{domain_role} or \code{language} arguments.}

\item{...}{Additional arguments passed to \code{ellmer::parallel_chat_structured}.}

\item{target}{Character. The entity or statement the stance is directed at.}

\item{chat_base}{An \code{ellmer::Chat} object or a list of up to 3 such objects.}

\item{type}{Analysis type: "object" (entity-based) or "statement" (proposition-based).}

\item{language}{Language code (e.g., "en", "ru"). If \code{NULL}, auto-detected.}

\item{scale}{Stance scale: "categorical", "numeric", or "likert".}

\item{domain_role}{Optional specific role for the domain expert (e.g., "historian").}

\item{prompts_dir}{Optional path to custom prompt templates.}

\item{verbose}{Logical. Show progress and summary table.}

\item{rpm}{Integer. Global requests per minute limit.}

\item{text}{The text column (if \code{x} is a data.frame) or character vector to analyse.}

\item{.output_col}{Name of the column where results will be stored.}
}
\value{
A \code{stance_result} object (for character method) or a modified
data frame (for data.frame method).

S3 object of class "stance_result" with:
\itemize{
\item summary: data.frame with results
\item analysis: expert analysis results
\item debates: debate results
\item judgements: final judgements
\item metadata: processing metadata
⁠@importFrom ellmer is_chat
}
}
\description{
\code{llm_stance()} implements a multi-stage "Collaborative rOle-infused LLM-based
Agents" (COLA) framework to detect stance in text towards a specific target
or statement.
It supports character vectors and data frames (compatible with \code{mall} style).

The \code{character} method performs the full three-stage analysis:
\enumerate{
\item \strong{Expert Analysis}: Linguistic, domain, and social media perspectives.
\item \strong{Stance Debates}: Parallel generation of arguments for, against, and neutral.
\item \strong{Judgement}: Final synthesis into a structured stance score.
}
}
\details{
\subsection{Chat Configuration Recommendations}{

For optimal results, consider the following requirements for each stage:
\itemize{
\item \strong{Expert Analysis (Stage 1)}: Requires excellent real-world
awareness and reasoning. Structured output is NOT required.
\item \strong{Stance Debates (Stage 2)}: Requires creativity and strong
writing skills. Structured output is NOT required.
\item \strong{Stance Judgement (Stage 3)}: Requires large context width,
superior reasoning, and \strong{MUST} support structured output (JSON mode).
}
}
}
\examples{
# result <- llm_stance(
#   x = "I support renewable energy",
#   target = "renewable energy",
#   chat_base = chat_model,
#   type = "object",
#   scale = "categorical"
# )
# results <- llm_stance(
#   x = tweets_df,
#   text = "text",
#   target = "climate_change",
#   chat_base = chat_model
# )
}
